version: "3.9"

services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: pie
      POSTGRES_PASSWORD: pie
      POSTGRES_DB: pie
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U pie -d pie"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  pgdata:
